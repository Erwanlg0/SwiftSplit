package com.elg.speedruncompanion.domain.model

enum class TimerDisplayColorToken {
    DEFAULT,
    AHEAD_GAINING,
    AHEAD_LOSING,
    BEHIND_LOSING,
    BEHIND_GAINING,
    BEST_SEGMENT,
    STATE_IDLE,
    STATE_RUNNING,
    STATE_PAUSED,
    STATE_FINISHED
}
